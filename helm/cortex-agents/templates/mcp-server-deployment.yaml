{{- if .Values.mcpServer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortex-mcp-server
  namespace: cortex-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cortex-mcp-server
  template:
    metadata:
      labels:
        app: cortex-mcp-server
    spec:
      containers:
        - name: cortex-mcp-server
          image: cortex/cortex-mcp-server:dev
          imagePullPolicy: IfNotPresent
          env:
            - name: SENTINEL_URL
              value: http://cortex-sentinel:8080
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: cortex-mcp-server
  namespace: cortex-system
spec:
  selector:
    app: cortex-mcp-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
{{- end }}
