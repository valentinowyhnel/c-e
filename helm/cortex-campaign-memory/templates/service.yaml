apiVersion: v1
kind: Service
metadata:
  name: cortex-campaign-memory
  namespace: cortex-system
spec:
  selector:
    app.kubernetes.io/name: cortex-campaign-memory
  ports:
    - name: http
      port: 8080
      targetPort: http
