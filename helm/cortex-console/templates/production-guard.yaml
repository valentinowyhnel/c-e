{{- if eq .Values.environment "prod" -}}
{{- if or (eq .Values.image.tag "dev") (eq .Values.image.tag "latest") (eq .Values.image.tag "") -}}
{{- fail "cortex-console: production deployment requires a non-dev immutable image tag" -}}
{{- end -}}
{{- if not .Values.internalApi.required -}}
{{- fail "cortex-console: production deployment requires internalApi.required=true" -}}
{{- end -}}
{{- if not .Values.consoleInternal.required -}}
{{- fail "cortex-console: production deployment requires consoleInternal.required=true" -}}
{{- end -}}
{{- end -}}
