apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortex-auth
  namespace: cortex-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cortex-auth
  template:
    metadata:
      labels:
        app: cortex-auth
    spec:
      containers:
        - name: auth
          image: cortex/cortex-auth:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: cortex-auth
  namespace: cortex-system
spec:
  selector:
    app: cortex-auth
  ports:
    - name: http
      port: 8080
      targetPort: 8080
