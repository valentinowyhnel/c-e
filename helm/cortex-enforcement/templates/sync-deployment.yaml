apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortex-sync
  namespace: cortex-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cortex-sync
  template:
    metadata:
      labels:
        app: cortex-sync
    spec:
      containers:
        - name: sync
          image: cortex/cortex-sync:dev
          imagePullPolicy: IfNotPresent
          env:
            - name: DATABASE_URL
              value: postgresql://cortex@cortex-postgres:5432/cortex
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: cortex-sync
  namespace: cortex-system
spec:
  selector:
    app: cortex-sync
  ports:
    - name: http
      port: 8080
      targetPort: 8080
