apiVersion: v1
kind: Service
metadata:
  name: cortex-mcp-server
  namespace: cortex-system
  labels:
    app.kubernetes.io/name: cortex-mcp-server
    app.kubernetes.io/part-of: cortex
spec:
  selector:
    app.kubernetes.io/name: cortex-mcp-server
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
