apiVersion: v1
kind: Service
metadata:
  name: cortex-priority-engine
  namespace: cortex-system
spec:
  selector:
    app.kubernetes.io/name: cortex-priority-engine
  ports:
    - name: http
      port: 8080
      targetPort: http
