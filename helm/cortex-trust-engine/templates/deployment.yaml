apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortex-trust-engine
  namespace: cortex-system
  labels:
    app.kubernetes.io/name: cortex-trust-engine
    app.kubernetes.io/part-of: cortex
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cortex-trust-engine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cortex-trust-engine
        app.kubernetes.io/part-of: cortex
    spec:
      containers:
        - name: cortex-trust-engine
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: CORTEX_ENVIRONMENT
              value: {{ .Values.environment | quote }}
            - name: CORTEX_INTERNAL_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.internalApi.secretName | quote }}
                  key: {{ .Values.internalApi.secretKey | quote }}
                  optional: {{ not .Values.internalApi.required }}
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
