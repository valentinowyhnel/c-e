[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cortex-policy-engine"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic==2.7.1",
]

[tool.uv]
dev-dependencies = [
    "pytest==8.1.1",
]
